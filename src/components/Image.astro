---
const {
  imageSrc = "/img/om1.webp",
  imageAlt = "Standard billedebeskrivelse",
  imageWidth,
  imageHeight
} = Astro.props;
---

<img width={imageWidth} height={imageHeight} class="w-full h-auto" src={imageSrc} alt={imageAlt} loading="lazy" />